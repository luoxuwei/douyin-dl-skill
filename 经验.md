# 经验日志

三个 Skill 共用。**开工前读一遍最近 20 条；遇到失败或用户纠正，解决完必须追加一条。** 只追加不删；过时的条目在末尾加"已失效：日期 原因"。

格式：

```
## [日期] <skill> · <一句话现象>
- 现象：出了什么错、什么输出
- 原因：查明的根因
- 解法：怎么绕过或修了什么（脚本、参数、文档）
- 影响：哪个脚本 / 哪个站点 / 什么条件下会再遇到
```

---

## [2026-09-25] video-dl · yt-dlp 下抖音报 403 "Fresh cookies are needed"

- 现象：`yt-dlp <抖音链接>` 返回 403，提示需要新鲜 cookie；`--cookies-from-browser` 又报 "Could not copy Chrome cookie database"。
- 原因：抖音网页 API 拒绝非浏览器客户端；浏览器开着时 cookie 库被锁，yt-dlp 读不了。
- 解法：不用 yt-dlp，改用无头 Edge 打开视频页，从网络响应截取 `douyinvod` 域的 mp4 流（画面和声音分开两路），带页面 cookie 下载后 ffmpeg 合并。落地为 `douyin-dl.mjs`。
- 影响：抖音全部走浏览器截流；`video-dl.mjs` 路由写死，不再尝试 yt-dlp。

## [2026-09-25] video-dl · 抖音同一视频截到 3 到 5 路流

- 现象：`streams captured: 5`，多个 part 是同一路流的不同分片请求。
- 原因：抖音播放器用不同 query 参数（range）重复请求同一媒体。
- 解法：按去掉 query 的路径去重，只保留每条路径第一次出现的完整 URL。
- 影响：`douyin-dl.mjs` 的 `page.on("response")`。

## [2026-09-26] video-dl · 抖音博主主页显示"用户不存在"或登录墙

- 现象：`/user/<id>` 页面正文"用户不存在"，或未登录时只有登录二维码，`a[href*="/video/"]` 一个都没有。
- 原因：一是 sec_uid 猜错了（不能从视频页的 `/user/self` 链接推）；二是主页要登录，单视频不用。
- 解法：sec_uid 从视频页的 `aweme/detail` 接口响应里取 `author.sec_uid`；主页下载前先 `login.mjs douyin`。
- 影响：`list-dl.mjs` 的抖音分支；`hasProfile()` 为假时直接提示登录。

## [2026-09-26] video-dl · 登录检测误报，窗口一开就关

- 现象：`login.mjs` 弹出浏览器后几秒就"login detected"关闭，用户来不及扫码。
- 原因：抖音未登录也发 `passport_csrf_token`、`ttwid`；小红书未登录也发 `web_session`、`a1`。用这些当登录标志会误判。
- 解法：只认真正登录后才出现的 cookie（抖音 `sessionid`，小红书 `customer-sso-sid`），并同时要求登录墙文字消失。
- 影响：`login.mjs` 的 `LOGIN_COOKIES` 和 `WALL_TEXT`；新增站点要先在匿名和登录两种状态下对比 cookie 再定标志。

## [2026-09-26] video-dl · 小红书扫码提示"重新扫码"

- 现象：扫码后小红书 App 提示重新扫码，网页不登录。
- 原因：puppeteer 默认带 `--enable-automation`，`navigator.webdriver` 为真，小红书风控在扫码确认阶段拦截。
- 解法：`launchOpts` 加 `ignoreDefaultArgs: ["--enable-automation"]` 和 `--disable-blink-features=AutomationControlled`。
- 影响：`common.mjs`；所有浏览器后端。

## [2026-09-26] video-dl · 登录浏览器没关，再启动报 "browser is already running for ...profile"

- 现象：`login.mjs` 或下载脚本启动失败，报 profile 被占用；用户看到的还是旧窗口。
- 原因：上一个登录进程仍持有 userDataDir；`taskkill /IM msedge.exe` 会连用户自己的 Edge 一起关，且有时关不掉。
- 解法：只结束命令行含 `video-dl` 的 msedge 进程（PowerShell `Get-CimInstance Win32_Process` 按 CommandLine 过滤）；从当前会话后台启动 `login.mjs`，不要用 `start cmd`（那个窗口没有 Node 的 PATH）。
- 影响：登录流程；SKILL.md 已写明。

## [2026-09-26] video-dl · 小红书笔记链接跳回首页

- 现象：`/explore/<id>` 打开后 URL 变成 `/explore`，截不到流。
- 原因：小红书笔记链接现在要带 `xsec_token` 和 `xsec_source` 参数；App 分享短链有时不带。
- 解法：让用户从浏览器地址栏复制完整链接；`list-dl.mjs` 收集主页链接时保留完整 href 含 query。
- 影响：`browser-dl.mjs`、`list-dl.mjs` 小红书分支。

## [2026-09-26] video-dl · B 站 UP 主主页 352 / 412

- 现象：`[BilibiliSpaceVideo] Request is rejected by server (352)` 或 `blocked by server (412)`。
- 原因：短时间内对同一 UP 主主页接口请求太多触发风控；`-J` 比 `--print` 更容易触发。
- 解法：`list-dl.mjs` 改用 `--flat-playlist --print`；触发后等几分钟，不要连续重试。
- 影响：B 站列表；同一会话里别反复探测。

## [2026-09-26] video-dl · YouTube 走本机代理 SSL EOF

- 现象：`[SSL: UNEXPECTED_EOF_WHILE_READING]`，重试三次都失败。
- 原因：本机代理对 Python 的 TLS 握手不稳，浏览器走同一代理却正常。
- 解法：不是脚本问题；换网络或临时关代理。脚本已加 `-R 5 --socket-timeout 30`。
- 影响：所有 yt-dlp 路径在这台机器上；其他机器可能没有。

## [2026-09-25] transcribe · Windows 上 ctranslate2 找不到 cublas64_12.dll

- 现象：`Library cublas64_12.dll is not found or cannot be loaded`，torch 明明能用 CUDA。
- 原因：ctranslate2 自己找 DLL，不走 torch 的路径；`os.add_dll_directory` 对它不够，要进 PATH。
- 解法：`transcribe.py` 启动时把 torch 的 `lib` 目录和 `nvidia/cublas/bin`、`nvidia/cudnn/bin` 加进 PATH；`setup-transcribe.ps1` 装 `nvidia-cublas-cu12`、`nvidia-cudnn-cu12`。
- 影响：Windows GPU 转写。

## [2026-09-25] transcribe · 人名书名同音字错误

- 现象：李沐写成李牧，赵世钰写成赵诗玉，Sutton 写成 Saturn。
- 原因：Whisper 靠声音，专有名词没上下文。
- 解法：`--prompt` 放人名书名术语，实测把赵诗玉、张伟南纠正为赵世钰、张伟楠；网络视频的文案和课程讲义是最好的 prompt 来源。转完仍要抽查。
- 影响：所有转写；`video-notes` 第 2 步要求先收集术语再转。

## [2026-09-25] transcribe · 中文输出夹半角标点和空格

- 现象：`马基雅维利, 你注定要...`，逗号后有空格。
- 原因：Whisper 按英文习惯输出标点；段落合并时用空格连接。
- 解法：`tidy()` 把 zh/ja 的半角标点换全角并去掉其后的空格；合并段落时前一段末尾是 CJK 字符就不加空格。
- 影响：`transcribe.py`。

## [2026-09-26] video-notes · ffmpeg drawtext 在 Windows 上不画字

- 现象：缩略图没有秒数标签，ffmpeg 静默回退。
- 原因：Windows 没有 fontconfig，drawtext 要显式 `fontfile`，且路径里的冒号要转义。
- 解法：`frames.mjs` 依次找 `C:/Windows/Fonts/arial.ttf` 等字体，`fontfile='C\:/Windows/Fonts/arial.ttf'`。
- 影响：`frames.mjs`。

## [2026-09-26] 通用 · Claude Code 不加载 junction 链接的 Skill 目录

- 现象：`~/.claude/skills/xxx` 是指向仓库的 junction，`/xxx` 提示 Unknown command。
- 原因：Skill 扫描跳过链接目录。
- 解法：复制成真实目录；改完仓库要重新复制。
- 影响：安装方式；README 已写。

## [2026-09-26] 通用 · git push 到 GitHub 走 HTTPS 时常 502

- 现象：`CONNECT tunnel failed, response 502`，API 却能通。
- 原因：本机代理对 github.com 的 git 协议不稳。
- 解法：SSH 走 443 端口（`~/.ssh/config` 里 `HostName ssh.github.com`、`Port 443`），一次就通。应急可用 GitHub Git Data API 逐文件上传。
- 影响：这台机器的所有 GitHub 仓库。
